General reposti!

##Tech used
- MongoDB
- Node
- NPM
- Apollo
- Create React
- GraphQL
- Express
- Material UI for skin

##TO RUN

###Run the Server
  - `cd /server`
  - `npm install`
  - `node start`
    -*runs on localhost:3000

###Run the Client
- `cd ../client`
- `npm install`
- `npm start`
  -*runs on localhost:3000
